spring:
  application:
    name: pessoaDataDogServiceApp

  datasource:
    url: jdbc:h2:mem:pessoa-db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  metrics:
    export:
      datadog:
        enabled: false
  datadog:
    metrics:
      export:
        enabled: false